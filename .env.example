# =============================================================================
# Litis — variables de entorno
# =============================================================================
# Copia este archivo como `.env.local` y llénalo. `.env.local` está en
# .gitignore y NUNCA debe subirse: la service role key salta toda la RLS.
# =============================================================================

# ── Supabase ────────────────────────────────────────────────────────────────
# Las NEXT_PUBLIC_* viajan al navegador. Es correcto: la anon key está pensada
# para eso y sin RLS no sirve de nada. La que no puede salir es la de abajo.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo servidor. Salta la RLS por completo. Se usa para el alta de despacho
# (cuando el usuario todavía no tiene membresía y no puede pasar ninguna
# política) y para el cron de alertas.
SUPABASE_SERVICE_ROLE_KEY=

# ── Sitio ───────────────────────────────────────────────────────────────────
# El origen de los enlaces de correo sale de AQUÍ, nunca del header `Host`:
# quien manda la petición controla ese header, y con él puede hacer que un
# correo legítimo de recuperación lleve el token a su propio servidor.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Correo (Resend) ─────────────────────────────────────────────────────────
# Sin API key el envío se simula y se escribe en consola: el flujo se puede
# probar entero sin cuenta.
RESEND_API_KEY=
# Remitente. Resend exige un dominio verificado; sin esto usa el de pruebas.
CORREO_REMITENTE=
# A dónde avisar cuando la corrida de alertas se rompe. Sin esto solo se
# escribe en la consola del servidor, que es donde nadie mira.
CORREO_ALERTAS=

# ── Cron de alertas de plazos ───────────────────────────────────────────────
# Protege GET /api/cron/alertas-plazos. Sin este valor el endpoint responde 503
# en vez de quedar abierto: un endpoint abierto que manda correo es un cañón de
# spam apuntando a los clientes del despacho.
#
#   openssl rand -base64 32
#
# El crontab del servidor:
#   0 7 * * * curl -fsS -H "Authorization: Bearer $CRON_SECRET" \
#     https://TU-DOMINIO/api/cron/alertas-plazos
CRON_SECRET=

# ── Cobro de la suscripción (Stripe) ────────────────────────────────────────
# Sin llaves el módulo corre en modo simulación: la pantalla de suscripción se
# recorre entera, NO se cobra nada y **el plan no cambia**. Se hace así a
# propósito — si la simulación activara el plan, un despliegue con la llave mal
# escrita regalaría el producto sin que nadie se enterara.
#
# Solo servidor. Nunca una llave publicable: el cobro va por Checkout
# hospedado, así que no corre Stripe.js en el navegador.
STRIPE_SECRET_KEY=

# El precio recurrente por asiento al mes (`price_...`). Sin esto el módulo
# también queda en simulación: cobrar sin saber cuánto, no.
#
# Ya está creado en la cuenta de Stripe de Litis, en modo REAL:
#   producto  prod_VBQnHcXM3sIe2J  «Litis — Despacho»
#   precio    price_1UB3wHRD2Fg2YJsu3660vmro  $390.00 MXN por asiento al mes
# Un id de precio no es un secreto (viaja en el propio Checkout); la llave sí.
#
# ⚠️ El precio quedó con `tax_behavior: unspecified`, o sea sin decidir si los
# $390 llevan IVA incluido o por encima. Es lo único reversible una sola vez en
# Stripe (de "sin decidir" a incluido o por encima, pero no entre esos dos), así
# que se decide antes de cobrarle a alguien. Y recuerda que **Stripe no es
# PAC**: el CFDI que va a pedir un despacho no lo timbra Stripe.
STRIPE_PRECIO_DESPACHO=

# Firma del webhook. Protege POST /api/webhooks/suscripcion, que es lo único
# que escribe el plan. Sin este valor el endpoint contesta 503 en vez de quedar
# abierto: sin firma que verificar, cualquiera que descubra la URL manda un
# JSON diciendo que su despacho tiene cincuenta asientos pagados.
#
# El endpoint escucha CUATRO eventos y ninguno más:
#   checkout.session.completed
#   customer.subscription.created / .updated / .deleted
STRIPE_WEBHOOK_SECRET=
